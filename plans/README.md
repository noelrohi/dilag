# Implementation Plans

Generated by the improve skill on 2026-07-07 (audit at commit `963c011`). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

The audit focused on quality-of-life improvements and "non-elegant" features (user request). Coverage: desktop app (renderer + Electron host), workspace tooling, product direction. Not audited in depth: `apps/web` marketing site internals, `packages/ui` component quality, dependency CVE posture.

## Execution order & status

| Plan | Title                                                                                                      | Priority | Effort                  | Depends on                                                      | Status                                                                                                                                          |
| ---- | ---------------------------------------------------------------------------------------------------------- | -------- | ----------------------- | --------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| 001  | Error-state recovery (no full-screen hijack, startup retry, always-visible session errors)                 | P1       | M                       | —                                                               | DONE (merged into main 2026-07-08, ff to 77ca6a2)                                                                                               |
| 004  | Real ESLint for desktop + shared packages                                                                  | P1       | M                       | —                                                               | DONE (merged into main 2026-07-08 @ 0461274)                                                                                                    |
| 006  | Pi session lifecycle: stop leaks, quit hook, question routing                                              | P1       | M                       | —                                                               | DONE (merged into main 2026-07-08 @ 810ff2f)                                                                                                    |
| 002  | ArrowUp prompt-history recall in composer                                                                  | P2       | S                       | 001 (same file)                                                 | DONE (merged into main 2026-07-08, ff to 907da9a)                                                                                               |
| 003  | Recent sessions on the project landing page                                                                | P2       | S                       | —                                                               | DONE (merged into main 2026-07-08 @ 9a35bd3)                                                                                                    |
| 005  | Workspace cleanups (session-create dedup, shared types, React catalog, turbo, dead surfaces, phantom dirs) | P2       | M                       | 004 (turbo.json/devDeps overlap)                                | DONE (merged into main 2026-07-08 @ 77d82a9; phantom dirs packages/db + packages/shared deleted from disk)                                      |
| 007  | SPIKE: editable screens (edit/rename/duplicate)                                                            | P2       | M                       | —                                                               | DONE (spike; design doc merged into main 2026-07-08 @ 0db15a1 → plans/007-editable-screens-DESIGN.md; build plan can be written from the doc)   |
| 008  | Import HTML onto the canvas                                                                                | P3       | M                       | 005 (bridge file overlap); shares write path with 007 if landed | DONE (merged into main 2026-07-08 @ bbe73ff)                                                                                                    |
| 009  | SPIKE: flows — screen-to-screen connections                                                                | P3       | M (spike) / L (feature) | —                                                               | DONE (spike; design doc merged into main 2026-07-08 @ 773681c → plans/009-flows-DESIGN.md; build gated on an Electron prototype of iframe drag) |
| 010  | One-click legacy session migration                                                                         | P3       | M                       | —                                                               | DONE (merged into main 2026-07-08, ff to 9b18559)                                                                                               |
| 011  | Editable screens BUILD (write/rename/duplicate, from 007's design doc)                                     | P2       | M-L                     | 008 (reuses its write helpers); spec in 007 DESIGN doc          | DONE (merged into main 2026-07-08, ff to b923a17)                                                                                               |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 after 001: both restructure `chat-view.tsx`; 001's changes are larger, rebase pain is one-directional.
- 005 after 004: both edit `turbo.json` and root/package devDeps; 004 defines the lint task shape 005 must not clobber.
- 008 after 005: both edit `packages/desktop-bridge/src/index.ts` and the IPC wiring files (005 deletes the dead `captureHtmlToImage` surface 008's exemplars sit next to).
- 007 and 008 both write into `.designs/` — whichever lands second should reuse the first's validated-write helper.
- 009's edge-endpoint stability rules should be cross-checked against 007 (rename/duplicate) and 008 (import) if those ship first.

## Findings considered and rejected / deferred (do not re-audit)

- **`screens/` → `.designs/` fallback sprawl**: checked — the fallback is cleanly centralized in `packages/desktop-bridge/src/generated-screen-policy.ts`; the documented legacy surface is not causing duplication. Retirement is gated on plan 010 baking.
- **Skipped runtime permission prompts**: documented product decision (`docs/platform.md:98`), not a finding.
- **React-Query-vs-Zustand split for session data**: documented as intentional in `use-sessions.ts:86-94`; only the _convention_ is under-specified (see deferred items).
- **Dead reconnection UI** (`reconnectAttempt` frozen at 0, unreachable "reconnecting" state): folded into plan 001 (removed there) rather than a separate wire-real-reconnect effort — in-process IPC makes spontaneous drops unlikely.
- **In-app skill authoring**: real friction (users hand-write skills in `~/.dilag/skills`; `routes/skills.tsx` only installs/toggles) but niche relative to the selected direction items — revisit after 007/008.
- **`sendMessage` full-store subscription re-renders** (`use-sessions.ts:123-133`): real but needs a profile to size; do after 001 changes settle. Candidate quick fix: `useShallow` selector.
- **Deferred larger refactors** (worth doing, not planned this round): split of 1430-line `electron/ipc/pi.ts` into runtime/providers/questions/event-mapping modules; per-method IPC arg-type dedup across bridge/host/preload (~30 methods); Zustand store convention convergence (6 stores, 2 styles, 2 homes); bridge-contract tests for `packages/desktop-bridge`; pre-commit hooks; desktop `.env.example`; type-aware lint rules (`no-floating-promises`) after plan 004 bakes.
