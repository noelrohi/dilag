# =============================================================================
# Dilag Web App Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values

# -----------------------------------------------------------------------------
# Better Auth
# -----------------------------------------------------------------------------
# Base URL for auth callbacks (no trailing slash)
BETTER_AUTH_URL=http://localhost:3000

# Secret for signing tokens (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET=

# -----------------------------------------------------------------------------
# Google OAuth
# https://console.cloud.google.com/apis/credentials
# -----------------------------------------------------------------------------
# Google OAuth Client ID
GOOGLE_CLIENT_ID=

# Google OAuth Client Secret
GOOGLE_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Polar.sh Configuration
# https://polar.sh/settings/tokens
# -----------------------------------------------------------------------------
# Organization Access Token (from Polar dashboard)
POLAR_ACCESS_TOKEN=

# Your Polar Organization ID
POLAR_ORG_ID=

# Product ID for the Pro license (from Polar dashboard)
# NEXT_PUBLIC_ prefix makes it available on both server and client
NEXT_PUBLIC_POLAR_PRODUCT_ID=

# Webhook secret for verifying Polar webhooks
# Create at: https://polar.sh/settings/webhooks
POLAR_WEBHOOK_SECRET=

# Environment: "sandbox" for testing, "production" for live
# Note: Sandbox and production have separate tokens, orgs, and products
POLAR_ENVIRONMENT=sandbox

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
# Local: postgresql://user:password@localhost:5432/dilag
# Neon/Supabase: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=
