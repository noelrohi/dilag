# Dilag Web App Environment Variables
# Marketing site currently does not require runtime secrets for core pages.

# Optional: override base URL for metadata/canonical links if needed.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
